<h2><%= current_user.name %>'s Readings</h2>

<% if @user_entries.blank? %>
  You do not have any readings.
<% else %>

  This Month:<br>
    Total readings: <%= @monthly_entries.size %><br>
    Total cards pulled: <%= @monthly_entries.size * 3 %><br>
    Major cards: <%= @monthly_entries.designation("Major").size %> - Minor cards: <%= @monthly_entries.designation("Minor").size %><br>
    <%= @monthly_entries.court_cards.size %>/<%= @monthly_entries.size * 3 %> court cards<br>
    # of cards with numbers for each number if pulled more than 3 times:<br>
    Cups: <%= @monthly_entries.suit_cards("Cups").size %>, Pentacles: <%= @monthly_entries.suit_cards("Pentacles").size %>, Swords: <%= @monthly_entries.suit_cards("Swords").size %>, Wands: <%= @monthly_entries.suit_cards("Wands").size %><br>


  <table>
    <% @user_entries.each do |entry| %>
      <tr>
        <td>
          <%= entry.category.name %>
        </td>
        <td>
          <%= link_to entry.created_at.strftime("%B %e, %Y"), entry_path(entry) %>
        </td>
        <td>
          <%= button_to "Delete Reading", entry_path(entry), method: :delete, onclick: "return confirm('Are you sure you want to delete this reading?')" %>
        </td>
      </tr>
    <% end %>
  </table>
<% end %>